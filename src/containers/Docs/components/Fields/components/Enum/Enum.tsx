import { DocLayout } from "@containers/Docs/shared/components"
import { ENUM } from "@modules/docs/domain/core/sections/field-types"

export default function Enum() {
  return (
    <DocLayout selected={ENUM}>
      <></>
    </DocLayout>
  )
}
